{{- if .Values.registry.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: vm-registry-htpasswd
  namespace: {{ .Values.registry.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "0"
type: Opaque
stringData:
  htpasswd: |
    {{ .Values.registry.auth.htpasswdHash }}
{{- end }}
