# `automation`

Ansible collection for this project's custom automation. `automation/gitops/` remains
the source of truth for cluster-wide infra; this collection handles per-tenant work.

Not published to Galaxy — referenced straight from this repo (git requirement).
